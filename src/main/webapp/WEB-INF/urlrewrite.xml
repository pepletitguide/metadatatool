<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE urlrewrite
    PUBLIC "-//tuckey.org//DTD UrlRewrite 4.0//EN"
    "http://www.tuckey.org/res/dtds/urlrewrite4.0.dtd">

<urlrewrite>

    <rule>
       <name>AngularJS redirection rule</name>
       <note>
       		This rule redirects Tomcat URLs to the AngularJS index page to let it handle the page redirection.
       		Must ignore file paths for resources to work (such as images, CSS, JS paths, etc) 
       </note>
       <from>^/public/([^.]+)/?$</from>
       <to type="redirect">/metadatatool/public#$1</to>
    </rule>


</urlrewrite>